# This is a simple example of a Python script that creates a list and appends numbers to it.
data_model = []

for i in range(10):
    data_model.append(i)
    print(data_model[i])
    
print (str(123))